import React, { Fragment } from "react";
import styles from "./MessageViewer.module.scss";

function MessageViewer() {
  return (
    <Fragment>
      <div className={styles.container}>Сообщения</div>
    </Fragment>
  );
}

export default MessageViewer;
